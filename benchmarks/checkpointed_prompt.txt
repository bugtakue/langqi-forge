You are participating in the Factory26 application-building benchmark.

Build the complete application described by the read-only requirements directory shown below. Start from this empty working directory and create exactly two runnable project directories at its root:

- frontend: must support `npm install` and `npm run build`
- backend: must support `PORT=<port> npm start` and expose `GET /api/health`

Requirements directory: {requirements_dir}
Domain: {domain}

Work autonomously until the application is complete. Implement persistent server-side state, backend authorization and validation, and the visible workflows in the requirements. Do not merely create static mock screens. Do not read, search for, infer from, or modify any benchmark test source. Do not access any directory other than this working directory and the requirements directory. Do not ask the user questions. Do not use network search, MCP, plugins, skills, prior sessions, or pre-existing project code. Package installation is allowed only inside the generated frontend/backend when needed.

Use recoverable delivery checkpoints:

1. Read the overview first, then immediately create both package manifests, a buildable frontend shell, and a backend health endpoint. Do not postpone the first runnable skeleton until after an exhaustive restatement of the requirements.
2. Run the clean install/build/start/health checks on that skeleton before broadening it.
3. Implement behavior in bounded increments. Each source-edit operation may change at most two files; land and syntax-check each increment before preparing the next. Never prepare the whole backend or the whole frontend as one monolithic edit.
4. Preserve a runnable state after every increment. Prioritize end-to-end persisted workflows over commentary, decorative breadth, or an unfinished giant source file.
5. After feature work, repeat clean frontend and backend install/build/start/health checks and fix integration failures before finishing.

A partial application, an explanation without runnable source, or a build that exists only in an uncommitted model response is a failed result.
